<!DOCTYPE html>
<html>
<head>
  <title>Login Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
    }

    .container {
      width: 500px;
      /* margin: 0 auto;
       */
      padding: 20px;
      margin-top: 214px;
      margin-left: 370px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      position: relative;
      animation: fade-in 0.5s ease-in-out;
    }

    @keyframes fade-in {
      0% {
        opacity: 0;
        transform: translateY(-20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .container h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    .cartoon-image {
      width: 120px;
      height: 120px;
      margin: 0 auto;
      background-image: url('cartoon_image.png'); /* Replace 'cartoon_image.png' with your own image */
      background-size: cover;
      border-radius: 50%;
      animation: bounce 1s infinite;
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    .form-group {
      margin-bottom: 15px;
      animation: slide-up 0.5s ease-in-out;
    }

    @keyframes slide-up {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .form-group label {
      display: block;
      margin-bottom: 5px;
    }

    .form-group input[type="email"],
    .form-group input[type="password"] {
      width: 95%;
      padding: 10px;
      border-radius: 5px;
      border: 1px solid #ccc;
      animation: slide-right 0.5s ease-in-out;
    }

    @keyframes slide-right {
      0% {
        opacity: 0;
        transform: translateX(-20px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .form-group button {
      width: 100%;
      padding: 10px;
      background-color: #4caf50;
      border: none;
      color: #fff;
      font-weight: bold;
      border-radius: 5px;
      cursor: pointer;
      animation: slide-left 0.5s ease-in-out;
    }

    @keyframes slide-left {
      0% {
        opacity: 0;
        transform: translateX(20px);
      }
      100% {
        opacity: 1;
        transform: translateX(0);
      }

      
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Reset Password</h2>
    <form action="/reset" method="POST" id="login-form">
        <div class="form-group">
          
        </div>
        <div class="form-group">
          <label for="password">Password:</label>
          <input type="password" id="password" name="password" required>
        </div>
      <div>
       <button type="submit" style=" border-radius: 10px; background-color: violet;" value="submit" name='submit'>login</button>
      </div>
      </form>  

      <button onclick="window.location.href='/login' ">Back</button>
</body>
</html>



   
